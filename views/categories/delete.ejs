<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Category</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body>
    <%- include('../partials/navbar') %>
    <div class="container">
        <h1>Delete Category</h1>
        <div class="alert alert-danger" role="alert">
            This category contains products. Please reassign or delete them first.
        </div>
        <form action="/categories/<%= category.id %>/delete" method="POST">
            <div class="mb-3">
                <label for="newCategory" class="form-label">Reassign products to another category:</label>
                <select class="form-select" id="newCategory" name="newCategoryId">
                    <option value="">Select a category</option>
                    <% categories.forEach(cat => { %>
                        <% if (cat.id !== category.id) { %>
                            <option value="<%= cat.id %>"><%= cat.name %></option>
                        <% } %>
                    <% }); %>
                </select>
            </div>
            <button type="submit" class="btn btn-danger">Reassign and Delete</button>
            <a href="/categories" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</body>
</html>
